import "./globals.css"; import type {Metadata} from "next"; import {Inter} from "next/font/google"; const inter=Inter({subsets:["latin"],display:"swap"}); export const metadata:Metadata={metadataBase:new URL(process.env.SITE_URL||"http://localhost:3000"),title:"Academia Demo VireMarca",description:"Performance, força e movimento.",manifest:"/manifest.webmanifest",themeColor:"#0A0A0B",openGraph:{title:"Academia Demo VireMarca",description:"Performance, força e movimento.",type:"website",locale:"pt_BR"}}; export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="pt-BR"><body className={inter.className}>{children}</body></html>}