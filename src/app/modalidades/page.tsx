import Link from "next/link";import {getPublicData} from "@/lib/queries/public";import {SmartImage} from "@/components/SmartImage";export default async function Page(){const d=await getPublicData();return <><header className="nav"><div className="container nav-inner"><Link href="/" className="brand">ACADEMIA<b> ●</b></Link><Link className="btn" href="/#trial">AULA GRÁTIS</Link></div></header><main className="page-hero"><div className="container"><span className="eyebrow">TREINO</span><h1>MODALIDADES</h1><p className="muted">Escolha o estímulo que combina com seu objetivo.</p><div className="grid grid-3" style={{marginTop:40}}>{d.modalities.map(x=><Link className="card" href={"/modalidades/"+x.slug} key={x.id}>{x.imageUrl?<SmartImage src={x.imageUrl} alt={x.imageAlt??x.name} section={x.name} className="media"/>:<div className="media smart-image-fallback"><span>{x.name}</span></div>}<h2>{x.name}</h2><p className="muted">{x.description??x.summary}</p><small>{x.durationMin?x.durationMin+" min · ":""}{x.level}</small></Link>)}</div></div></main></>}